pluginManagement {
  repositories {
      mavenLocal()
      gradlePluginPortal()
      maven {
          credentials {
              username mavenUser
              password mavenPassword
          }
          url mavenRepo
    }
  }
}

rootProject.name = 'auto-value-yaml'
